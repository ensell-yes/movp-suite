{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["SUPABASE_WORKDIR"],
  "tasks": {
    "test": { "dependsOn": ["^test"] },
    "typecheck": { "dependsOn": ["^typecheck"] }
  }
}
